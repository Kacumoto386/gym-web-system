# 会员照片 · 摄像头拍照功能方案

## V3.2.0 | 2026-05-09

---

## 1. 需求概述

### 1.1 当前状态

会员编辑弹窗已有照片功能：
- ✅ **本地上传**（`<input type="file">` 选择图片 → `POST /api/members/{id}/photo`）
- ✅ **照片预览**（`GET /api/members/{id}/photo` → 加载已保存照片）
- ✅ **删除照片**（`DELETE /api/members/{id}/photo`）

### 1.2 本次需求

在现有"上传"按钮旁边，增加**摄像头拍照**按钮：
- 点击后弹出摄像头实时预览窗口
- 拍照后生成图片，自动上传到后端
- 拍照窗口关闭后，编辑弹窗自动刷新显示新照片
- 纯前端实现（浏览器 `getUserMedia` API），无需后端改动

### 1.3 为什么纯前端？

后端 `POST /api/members/{id}/photo` 接口已经存在且接收 `multipart/form-data`。摄像头拍照只是获取图片的一种前端方式 — 拍完后用同样的接口上传即可，后端完全不需要改动。

---

## 2. UI 设计

### 2.1 照片区域布局变化

**修改前**（双按钮）：
```
┌─────────────────────┐
│    📷 暂无照片       │  ← photoArea (w-40 h-48)
├─────────────────────┤
│  📤 上传             │  ← 单个按钮
└─────────────────────┘
```

**修改后**（三按钮）：
```
┌─────────────────────┐
│    📷 暂无照片       │  ← photoArea (不变)
├─────────────────────┤
│  📤 上传  │  📸 拍照  │  ← 并排两个按钮
│  (删除按钮)           │
└─────────────────────┘
```

### 2.2 摄像头弹窗

点击"📸 拍照"后弹出模态窗口：

```
┌──────────────────────────────────────────┐
│  📸 拍照  × (关闭按钮)                    │
├──────────────────────────────────────────┤
│                                          │
│          ┌──────────────────────┐         │
│          │                      │         │
│          │   摄像头实时预览       │         │
│          │   <video> 元素        │         │
│          │   640×480             │         │
│          │                      │         │
│          └──────────────────────┘         │
│                                          │
│   ┌────────────┐   ┌────────────┐       │
│   │  📸 拍照    │   │  ✕ 取消    │       │  ← 底部按钮
│   └────────────┘   └────────────┘       │
│                                          │
│   ┌──── 预览区 ────┐                      │  ← 拍照后显示
│   │   刚拍的照片    │                      │
│   └────────────────┘                      │
│                                          │
└──────────────────────────────────────────┘
```

### 2.3 拍照后流程

```
点击"📸 拍照" → 弹出摄像头窗口 → 显示实时预览
    ↓
点击"📸 拍照"按钮 → canvas.drawImage() 截取 frame
    ↓
canvas → blob → FormData → POST /api/members/{id}/photo
    ↓
上传成功 → 关闭摄像头窗口 → 刷新编辑弹窗的照片预览
```

---

## 3. 技术方案

### 3.1 前端代码（全部在 `members.html`）

#### 新增 HTML 结构

在文件末尾（`</script>` 之后），新增摄像头弹窗的 HTML：

```html
<!-- 摄像头拍照弹窗 -->
<div id="cameraModal" class="hidden fixed inset-0 bg-black/40 z-50 flex items-center justify-center">
    <div class="bg-white rounded-xl shadow-xl w-full max-w-lg mx-4 p-6">
        <div class="flex justify-between items-center mb-4">
            <h3 class="text-lg font-semibold">📸 拍照</h3>
            <button onclick="closeCamera()"
                    class="text-gray-400 hover:text-gray-600 text-xl">&times;</button>
        </div>

        <!-- 视频预览 -->
        <div id="cameraPreview" class="bg-black rounded-lg overflow-hidden mb-4">
            <video id="cameraVideo" autoplay playsinline
                   class="w-full h-80 object-cover"></video>
        </div>

        <!-- 拍照预览（拍照后显示） -->
        <canvas id="cameraCanvas" class="hidden w-full h-80 object-cover rounded-lg mb-4"></canvas>

        <!-- 状态提示 -->
        <div id="cameraStatus" class="text-sm text-center hidden mb-3"></div>

        <!-- 按钮 -->
        <div class="flex justify-center gap-3">
            <button id="cameraCaptureBtn" onclick="capturePhoto()"
                    class="px-6 py-2 text-sm text-white bg-blue-600 rounded-lg hover:bg-blue-700">
                📸 拍照
            </button>
            <button id="cameraRetakeBtn" onclick="retakePhoto()"
                    class="hidden px-6 py-2 text-sm text-gray-600 border rounded-lg hover:bg-gray-50">
                🔄 重拍
            </button>
            <button onclick="closeCamera()"
                    class="px-6 py-2 text-sm text-gray-600 border rounded-lg hover:bg-gray-50">
                ✕ 关闭
            </button>
        </div>
    </div>
</div>
```

#### 新增 JavaScript 函数

```javascript
// ════════════════════════════════════════════
// 摄像头拍照
// ════════════════════════════════════════════

let cameraStream = null;  // 当前摄像头流

function openCamera() {
    var modal = document.getElementById('cameraModal');
    var video = document.getElementById('cameraVideo');
    var canvas = document.getElementById('cameraCanvas');
    var captureBtn = document.getElementById('cameraCaptureBtn');
    var retakeBtn = document.getElementById('cameraRetakeBtn');
    var statusEl = document.getElementById('cameraStatus');

    // 重置状态
    canvas.classList.add('hidden');
    captureBtn.classList.remove('hidden');
    retakeBtn.classList.add('hidden');
    statusEl.classList.add('hidden');
    statusEl.textContent = '';

    modal.classList.remove('hidden');

    // 启动摄像头
    navigator.mediaDevices.getUserMedia({ 
        video: { 
            width: { ideal: 640 },
            height: { ideal: 480 },
            facingMode: 'environment'  // 优先后置摄像头
        }, 
        audio: false 
    })
    .then(function(stream) {
        cameraStream = stream;
        video.srcObject = stream;
        video.play();
    })
    .catch(function(err) {
        statusEl.classList.remove('hidden');
        statusEl.className = 'text-sm text-center text-red-500 mb-3';
        if (err.name === 'NotAllowedError') {
            statusEl.textContent = '❌ 摄像头权限被拒绝，请在浏览器设置中允许摄像头访问';
        } else if (err.name === 'NotFoundError') {
            statusEl.textContent = '❌ 未检测到摄像头设备';
        } else {
            statusEl.textContent = '❌ 摄像头启动失败: ' + err.message;
        }
    });
}

function capturePhoto() {
    var video = document.getElementById('cameraVideo');
    var canvas = document.getElementById('cameraCanvas');
    var captureBtn = document.getElementById('cameraCaptureBtn');
    var retakeBtn = document.getElementById('cameraRetakeBtn');
    var statusEl = document.getElementById('cameraStatus');

    // 设置 canvas 尺寸匹配视频
    canvas.width = video.videoWidth || 640;
    canvas.height = video.videoHeight || 480;

    // 从视频帧截取
    var ctx = canvas.getContext('2d');
    ctx.drawImage(video, 0, 0, canvas.width, canvas.height);

    // 显示截取结果，隐藏视频
    canvas.classList.remove('hidden');
    video.style.display = 'none';
    captureBtn.classList.add('hidden');
    retakeBtn.classList.remove('hidden');

    // 自动上传
    statusEl.classList.remove('hidden');
    statusEl.className = 'text-sm text-center text-blue-500 mb-3';
    statusEl.textContent = '⏳ 上传中...';

    canvas.toBlob(function(blob) {
        var formData = new FormData();
        formData.append('file', blob, currentEditMemberId + '.jpg');

        fetch('/api/members/' + currentEditMemberId + '/photo', {
            method: 'POST',
            body: formData,
        })
        .then(function(r) { 
            return r.ok ? r.json() : r.json().then(function(j) { throw new Error(j.detail || '上传失败'); });
        })
        .then(function(res) {
            if (res.success) {
                statusEl.className = 'text-sm text-center text-green-600 mb-3';
                statusEl.textContent = '✅ 拍照上传成功！';
                // 刷新编辑弹窗的照片
                loadPhoto(currentEditMemberId);
                // 2秒后自动关闭
                setTimeout(function() {
                    closeCamera();
                }, 2000);
            }
        })
        .catch(function(err) {
            statusEl.className = 'text-sm text-center text-red-500 mb-3';
            statusEl.textContent = '❌ 上传失败: ' + err.message;
        });
    }, 'image/jpeg', 0.9);
}

function retakePhoto() {
    var video = document.getElementById('cameraVideo');
    var canvas = document.getElementById('cameraCanvas');
    var captureBtn = document.getElementById('cameraCaptureBtn');
    var retakeBtn = document.getElementById('cameraRetakeBtn');
    var statusEl = document.getElementById('cameraStatus');

    // 恢复视频预览
    canvas.classList.add('hidden');
    video.style.display = 'block';
    captureBtn.classList.remove('hidden');
    retakeBtn.classList.add('hidden');
    statusEl.classList.add('hidden');
    statusEl.textContent = '';
}

function closeCamera() {
    var modal = document.getElementById('cameraModal');

    // 关闭摄像头
    if (cameraStream) {
        cameraStream.getTracks().forEach(function(track) { track.stop(); });
        cameraStream = null;
    }

    // 重置 video
    var video = document.getElementById('cameraVideo');
    video.srcObject = null;
    video.style.display = 'block';

    modal.classList.add('hidden');
}
```

### 3.2 后端改动

**无需改动。** 拍照上传复用已有的 `POST /api/members/{member_id}/photo` 端点。

---

## 4. 依赖

| 依赖 | 类型 | 说明 |
|------|------|------|
| 浏览器 `getUserMedia` | Web API | 所有现代浏览器（Chrome/Firefox/Safari/Edge）均内置支持 |
| `canvas.toBlob()` | Web API | 将 Canvas 内容转为 Blob 用于上传 |
| `FormData` | Web API | 上传文件的标准方式 |
| HTTPS/本地环境 | 环境要求 | `getUserMedia` 需要 HTTPS 或 localhost |

> ⚠️ `getUserMedia` 要求：
> - 生产环境必须通过 HTTPS 访问
> - 开发环境 `http://127.0.0.1:8000` 也可以（浏览器视其为安全上下文）
> - 用户首次使用时浏览器会弹出权限询问框

---

## 5. 文件变更清单

| 文件 | 操作 | 说明 |
|------|:----:|------|
| `backend/app.py` | 修改 | 版本号 3.1.9 → 3.2.0 |
| `frontend/templates/members.html` | 修改 | 照片区新增"📸 拍照"按钮；新增摄像头弹窗 HTML；新增 JS 函数 |

---

## 6. 界面交互流程

```
1. 打开会员编辑弹窗
   └→ 加载照片（文件路径或空占位）
   └→ 显示"📤 上传"和"📸 拍照"按钮

2. 点击"📸 拍照"
   └→ 浏览器弹出摄像头权限弹窗
   ├── 用户允许 → 显示摄像头实时预览
   └── 用户拒绝 → 显示错误提示"摄像头权限被拒绝"

3. 点击"📸 拍照"按钮（在摄像头弹窗内）
   └→ canvas.drawImage() 截取帧
   └→ 预览区显示刚拍的照片
   └→ 自动 toBlob() + FormData 上传
   ├── 上传成功 → 显示"✅ 拍照上传成功"
   │   └→ 刷新编辑弹窗照片预览
   │   └→ 2秒后自动关闭弹窗
   └── 上传失败 → 显示错误信息
       └→ 可重新拍照/关闭

4. 关闭摄像头弹窗
   └→ 停止摄像头流（释放资源）
   └→ 恢复 modal 到初始状态
```

---

## 7. 验证方法

| # | 测试场景 | 预期结果 |
|:-:|----------|----------|
| 1 | 点击"📸 拍照"（有摄像头） | 弹窗弹出，显示实时视频 |
| 2 | 点击"📸 拍照"（无摄像头） | 显示"未检测到摄像头设备" |
| 3 | 拒绝摄像头权限 | 显示"摄像头权限被拒绝" |
| 4 | 点击弹窗拍照按钮 | 拍照成功，显示预览，自动上传 |
| 5 | 上传成功后 | 编辑弹窗自动刷新为新照片 |
| 6 | 点击"重拍" | 恢复实时预览，可重新拍照 |
| 7 | 关闭弹窗 | 摄像头关闭，资源释放 |
| 8 | 多次打开/关闭 | 每次正常启动和关闭 |

---

## 8. 注意事项

1. **摄像头权限**：首次使用浏览器会弹出权限询问，用户必须允许
2. **后置摄像头**：`facingMode: 'environment'` 优先使用后置摄像头（适合拍照），如果设备只有前置则自动回退
3. **图片质量**：使用 JPEG 格式，质量 0.9（平衡清晰度和文件大小），后端会自动裁剪为 360×480
4. **资源释放**：关闭弹窗时必须 `track.stop()` 释放摄像头，否则其他应用无法使用
5. **无法拍照的后备**：如果 `getUserMedia` 不工作（如非 HTTPS 环境），用户仍可使用"📤 上传"按钮

---

*本文档记录了 V3.2.0 摄像头拍照功能的完整设计方案。*
